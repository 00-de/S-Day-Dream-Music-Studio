SDayDream Music Studio — Phase 1（2ファイル構成）

【Vercelに上げる場合】
1. この2つ（index.html / app.js）を同じフォルダに入れる
2. Vercel のプロジェクト画面にフォルダごとドラッグ&ドロップ
   ※ index.html と app.js が同じ階層にあることが大事です
   ※ Framework Preset は「Other」を選んでください

【PCだけで動かす場合】
index.html を右クリック →「プログラムから開く」→ Chrome / Edge
（app.js を同じフォルダに置いたままにしてください）
